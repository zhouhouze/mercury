# C Contracts

C must use the existing public artifact rule:

```text
ArtifactRecord(type="mindmap", metadata.format="mermaid")
```

`nodeSourceMap` belongs in artifact metadata. Do not add a top-level artifact field without V1.2-0 review.

