# A Tests

Required tests:

- contract tests for `StructuredPageContext`.
- fixture tests for article, docs, and README pages.
- edge tests for empty readable content.
- deterministic ID stability tests.
- annotation fallback tests.

