# Page Reading Module

Active development documents live in:

```text
docs/active/modules/runtime/page_reading/
```

This directory should contain implementation code, fixtures, runtime adapters, and tests only.
