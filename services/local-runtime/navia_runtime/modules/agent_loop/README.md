# Agent Loop Module

Active development documents live in:

```text
docs/active/modules/runtime/agent_loop/
```

This directory should contain AgentCore/CoreProvider implementation code, fixtures, runtime adapters, and tests only.
