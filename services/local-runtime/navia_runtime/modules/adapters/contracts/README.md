# D Adapter Contracts

Adapter contracts are defined in `docs/navia_v1_project_docs/contracts/v1_2_adapter_contracts.md`.

Local contract files may add implementation helpers but must not redefine:

- `AdapterSpec`.
- `AdapterInvocation`.
- `AdapterResult`.

