# Adapter Module

Active development documents live in:

```text
docs/active/modules/runtime/adapters/
```

This directory should contain adapter implementation code, fixtures, runtime adapters, and tests only.
