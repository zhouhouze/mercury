# Mixed Monorepo Snapshot

Local reproducible V1.16 fixture representing a mixed service and frontend repository.

The validation command is intentionally blocked unless a separate allowlist audit expands command policy.
