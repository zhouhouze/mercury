# V1.2-B Chat Renderer Workspace 门禁

版本：V1.2-B Stage Gate Draft
日期：2026-06-04
负责模块：B，结构化数据、流式文本和 Mindmap 前端实时渲染

---

## 1. 阶段目标

将 Chat 页签拆成纯前端渲染模块，消费 D 输出的 SSE 和 ArtifactRecord，展示结构化总结、流式回答、工具状态、Mindmap 和 Debug 信息。

---

## 2. 允许工作区

B 模块 Codex 只允许修改：

```text
apps/chrome-extension/src/modules/chat_renderer/
apps/chrome-extension/src/modules/artifact_renderer/
apps/chrome-extension/src/modules/debug_renderer/
apps/chrome-extension/src/modules/mindmap_renderer/
docs/navia_v1_project_docs/stage-gates/v1.2-b-chat-renderer.md
```

禁止修改 Runtime AgentCore、AdapterRegistry、Mindmap generation、Page Reading 合同、`injectedPanel.ts`、`sse.ts` 和 E2E 入口。既有前端入口接入由 Integration Codex 负责。

---

## 3. 开发任务

- [ ] 先确认四个 renderer 的 `docs/public-api.md`、`docs/executable-contract.md`、`docs/fixture-spec.md`、`docs/test-and-evidence-plan.md` 已满足自动化开发需要。
- [ ] 拆出 stream renderer。
- [ ] 拆出 artifact renderer。
- [ ] 拆出 debug renderer。
- [ ] 渲染 structured summary。
- [ ] 渲染 paragraph annotation 展示片段。
- [ ] 渲染 Mermaid artifact。
- [ ] Mermaid 渲染失败时展示 source fallback。
- [ ] Mindmap 节点点击后按 source map 高亮源段落或展示 excerpt fallback。
- [ ] 未知 SSE event 不崩溃。

---

## 4. 验收标准

- [ ] 前端只消费 `/v1/chat/stream` SSE。
- [ ] 使用 recorded SSE / artifact fixtures 生成 renderer evidence。
- [ ] `response.delta` 逐步追加。
- [ ] `artifact.created` 可渲染 summary / answer / mindmap。
- [ ] Runtime offline 可见。
- [ ] PageContext missing 可见。
- [ ] tool failure 可见。
- [ ] B 不直接调用 A/C/MCP/Skill/API。

---

## 5. 预审计

当前结论：

```text
Ready for ChatGPT audit after V1.2-0 final documentation alignment.
Go for implementation only after audit closes all fatal/major issues.
```

No-Go：

- B 直接生成 summary、answer 或 mindmap。
- B 绕过 D 调用 MCP / Skill / API。
- B 保存 AgentCore 状态作为事实源。
- B 修改 Runtime 合同。
