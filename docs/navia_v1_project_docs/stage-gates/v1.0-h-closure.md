# V1.0-H Stage Gate：Closure / Regression / Documentation

版本：V1.0-H Gate  
日期：2026-06-02  
阶段状态：自动回归与文档同步已完成；V1 complete 等待真实 Chrome UI 最终验收

---

## 1. 阶段目标

V1.0-H 的目标是收口 V1，不扩大产品声明，并确认剩余阻塞是否属于代码问题、验收问题或外部权限问题。

V1 complete 只能声明：

```text
Installable Chrome Side Panel Chatbox and current-page companion reading with controlled single-session AgentCore are ready.
```

---

## 2. PRD 规格检视

对齐项：

- Chrome Side Panel Chatbox。
- 当前网页 PageContext。
- 摘要 / 问答 / Mermaid mindmap。
- Headless Runtime。
- 单 Session AgentCore。
- 状态机、治理、事件、trace。
- Session refresh recovery。

未纳入 V1 complete：

- V1.0-F 语音输入。
- 网页贴边悬浮球。
- 站内嵌入面板。
- RAG / 长期记忆 / MCP / Skill / 多 Agent。
- 深度研究 / PPT / 桌宠 / 云同步。

---

## 3. 回归计划

自动回归：

- Runtime tests。
- Chrome extension tests。
- TypeScript typecheck。
- WXT build。
- Chrome unpacked artifact sync。
- Git ignored artifact scan。

真实 UI 验收：

```text
Load unpacked apps/chrome-extension/chrome-mv3-unpacked
-> 启动 Runtime
-> 打开真实网页
-> Side Panel online
-> 读取并提交当前页面
-> 点击“总结”
-> 输入页面问题并发送
-> 点击“Mindmap”
-> 确认 Mermaid 渲染或 source fallback
-> 刷新 Side Panel
-> 确认最近 session、页面信息、历史消息和 artifact 恢复
```

---

## 4. 自动验收报告

已通过：

```text
Runtime tests: 21 passed
Chrome extension tests: 2 files / 3 tests passed
Typecheck: passed
WXT build: passed
HTTP E2E: sessions -> page/context -> chat/stream -> restore -> trace passed
```

已同步：

- `README.md`
- `services/local-runtime/README.md`
- `docs/navia_v1_project_docs/06-api-contract.md`
- `apps/chrome-extension/chrome-mv3-unpacked`

---

## 5. PRD 规格复检

通过项：

- 未扩大 V1 声明。
- 未引入 V1 非目标。
- Runtime 仍是 AgentCore 状态源。
- Side Panel 只缓存 `session_id`。
- Session restore 和 trace 均由 Runtime 提供。

剩余阻塞：

- 真实 Chrome UI 最终验收尚未在本阶段自动完成。
- GitHub 远端推送仍依赖仓库写权限；此前本机凭据无 `zhouhouze/mercury` 写权限。

---

## 6. 最终结论

```text
V1.0-H 自动回归与文档同步已完成。
当前不能声明 V1 complete。
剩余阻塞是高风险人工验收：真实 Chrome UI 最终验收，以及可选的 GitHub 写权限确认。
```
