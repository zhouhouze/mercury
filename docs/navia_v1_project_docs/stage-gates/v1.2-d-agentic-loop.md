# V1.2-D CoreProvider / Adapter Workspace 门禁

版本：V1.2-D Stage Gate Draft
日期：2026-06-04
负责模块：D，CoreProvider 适配层、ChatBox turn 编排边界与 MCP / Skill / API Adapter 编排

---

## 1. 阶段目标

将 D 模块定义为 Navia Runtime 与可替换 Agent Core Provider 之间的稳定适配层。V1.2 首选 Core Provider 为 piAgent；D 负责 CoreProvider 抽象、AdapterRegistry、GovernanceBridge、ToolResult / ArtifactRecord / AgentEvent / Trace 映射，并继续保证 state、budget、permission、SSE 和 Trace 可审计。

D 阶段必须 mock-first。真实 `piAgentProvider` 接入在 V1.2-D 内默认 No-Go，除非 piAgent 仓库、版本或 commit、license、运行时、工具调用模型和状态模型已经单独审计并登记。

---

## 2. 允许工作区

D 模块 Codex 只允许修改：

```text
services/local-runtime/navia_runtime/modules/agent_loop/
services/local-runtime/navia_runtime/modules/adapters/
docs/navia_v1_project_docs/stage-gates/v1.2-d-agentic-loop.md
```

禁止修改 B UI 实现、A/C 输出合同、公共 API 合同、`agent.py` 和 `tools.py` 既有入口。需要接入既有 Runtime 时由 Integration Codex wiring；需要变更合同时回到 V1.2-0。

---

## 3. 进入开发前门禁

- [ ] `CoreProvider`、`CoreTurnInput`、`CoreTurnResult` public API 已冻结。
- [ ] AdapterSpec / AdapterInvocation / AdapterResult 与 `contracts/v1_2_adapter_contracts.md` 一致。
- [ ] GovernanceBridge 的预算、权限、PreToolUse、PostToolUse 顺序已写入 executable contract。
- [ ] MockCoreProvider fixture 覆盖 summary、QA、mindmap、denied、missing activePage、budget exceeded。
- [ ] real piAgentProvider 依赖锁未完成时，只允许写 adapter contract 和 config 文档，不允许实现真实调用。

---

## 4. 开发任务

- [ ] 先确认 CoreProvider / Agent Loop / Adapters 的 `docs/public-api.md`、`docs/executable-contract.md`、`docs/fixture-spec.md`、`docs/test-and-evidence-plan.md` 已满足自动化开发需要。
- [ ] 保留 public `TurnRunner` 或 Integration 兼容入口。
- [ ] D-V1.0-0：冻结 CoreProvider / Adapter / Governance / Event / Trace fixture。
- [ ] D-V1.0-1：实现 `CoreTurnInput` builder，包含 activePage、recentMessages、budget、adapterSpecs。
- [ ] D-V1.0-2：实现 `MockCoreProvider`，输出 deterministic CoreTurnResult。
- [ ] D-V1.0-3：实现 AdapterRegistry 和 safe internal adapter placeholder。
- [ ] D-V1.0-4：实现 GovernanceBridge，确保 budget/permission 在 `tool.started` 前阻断。
- [ ] D-V1.0-5：将 adapter result 映射为 ToolResult、ArtifactRecord request、AgentEvent 和 trace evidence。
- [ ] D-V1.0-6：实现 missing activePage、denied adapter、budget exceeded、tool failure 的错误路径。
- [ ] D-V1.0-7：完成 provider config 文档：`NAVIA_AGENT_CORE_PROVIDER=mock | piagent | custom`，默认 mock。
- [ ] D-V1.0-8：piAgentProvider 只完成 contract stub 和 dependency lock checklist。

---

## 5. 验收标准

- [ ] 每个 user message 创建 `turn_id`。
- [ ] 使用 fake adapters 和 activePage fixtures 生成 turn event evidence。
- [ ] 每个 adapter call 创建 `tool_call_id`。
- [ ] Budget / Permission check 在 `tool.started` 前执行。
- [ ] denied adapter 不产生 `tool.started`。
- [ ] missing activePage 返回 `PAGE_CONTEXT_REQUIRED`，不产生假 artifact。
- [ ] budget exceeded 后不继续 model/tool/adapter call。
- [ ] adapter failure 映射为 `tool.failed` 和 visible error 所需字段。
- [ ] MCP / Skill / API 不绕过 D。
- [ ] piAgentProvider 不直接写 ArtifactRecord、SSE、EventStore 或 UI。
- [ ] `NAVIA_AGENT_CORE_PROVIDER=mock | piagent | custom` 的配置策略已在文档中闭环。
- [ ] trace 可按 `turn_id` 看到 state、intent、budget、tool、artifact、response、error。
- [ ] D 不实现长期记忆、RAG、多 Agent 或浏览器自动操作。

---

## 6. 验收证据

必须登记：

- `summary_turn.events.json`
- `qa_turn.events.json`
- `mindmap_turn.events.json`
- `denied_adapter.events.json`
- `missing_active_page.events.json`
- `budget_exceeded.events.json`
- trace reconstruction summary
- provider config evidence
- piAgent dependency lock checklist status

建议命令：

```bash
PYTHONPATH=services/local-runtime python3 -m pytest -q services/local-runtime/navia_runtime/modules/agent_loop/tests services/local-runtime/navia_runtime/modules/adapters/tests
```

---

## 7. 预审计

当前结论：

```text
Ready for ChatGPT audit after V1.2-0R readiness closure.
Go for mock-first CoreProvider / Adapter Layer implementation only after audit closes all fatal/major issues.
No-Go for real piAgentProvider implementation until piAgent repository/version/license lock is complete.
```

No-Go：

- D 默认执行高风险 MCP / Skill side effect。
- D 引入长期记忆或 RAG。
- D 允许前端直连 adapter。
- D 允许 AdapterResult 跳过 ToolResult envelope。
- piAgent 或其他 CoreProvider 绕过 D Adapter Layer 直接写结果。
- piAgent 具体仓库、版本或 license 未锁定就进入实质接入。
- D 用 real provider 不稳定结果替代 deterministic mock contract tests。

---

## 8. 本轮执行记录：D-V1.0-1/2/3/4/5/6/7/8 mock-first 闭环

日期：2026-06-04

本轮完成范围：

- 在 `services/local-runtime/navia_runtime/modules/adapters/runtime/` 内新增 `AdapterRegistry`、safe internal adapters、deny/failure fixtures 和 `AdapterResult` envelope。
- 在 `services/local-runtime/navia_runtime/modules/agent_loop/runtime/` 内新增 `MockCoreProvider`、`GovernanceBridge`、`run_agentic_turn(input)` 和 trace summary。
- `NAVIA_AGENT_CORE_PROVIDER` 策略保持文档闭环：V1.2-D 只实现 `mock`；`piagent` 和 `custom` 仍为 contract-only / No-Go，直到 dependency lock 审计完成。
- D 复用既有 `AgentEventType`，不新增 ad-hoc event string；adapter failure 映射为 `tool.done(status="failed")` + `error`。
- Budget / Permission / activePage check 在 `tool.started` 前执行。
- 成功 adapter result 映射为 `ToolResult`、`ArtifactRecord` request、`artifact.created` event 和 trace evidence。
- Denied、missing activePage、budget exceeded 不产生 `tool.started` 或 artifact。
- D 不修改 B UI、A/C 输出、公共 API、`agent.py`、`tools.py` 或 `app.py`。

已执行验收：

```bash
PYTHONPATH=services/local-runtime python3 -m pytest -q services/local-runtime/navia_runtime/modules/agent_loop/tests services/local-runtime/navia_runtime/modules/adapters/tests
PYTHONPATH=services/local-runtime python3 -m pytest -q services/local-runtime/navia_runtime/modules/page_reading/tests services/local-runtime/navia_runtime/modules/agent_loop/tests services/local-runtime/navia_runtime/modules/adapters/tests
PYTHONPATH=services/local-runtime python3 -m pytest -q services/local-runtime/tests
git diff --check
```

结果：

```text
8 passed
16 passed
21 passed
git diff --check passed
```

证据路径：

```text
services/local-runtime/navia_runtime/modules/agent_loop/tests/evidence/summary_turn.events.json
services/local-runtime/navia_runtime/modules/agent_loop/tests/evidence/qa_turn.events.json
services/local-runtime/navia_runtime/modules/agent_loop/tests/evidence/mindmap_turn.events.json
services/local-runtime/navia_runtime/modules/agent_loop/tests/evidence/denied_adapter.events.json
services/local-runtime/navia_runtime/modules/agent_loop/tests/evidence/missing_active_page.events.json
services/local-runtime/navia_runtime/modules/agent_loop/tests/evidence/budget_exceeded.events.json
services/local-runtime/navia_runtime/modules/agent_loop/tests/evidence/adapter_failure.events.json
```

PRD 规格复检：

```text
Pass for D module scope.
D 只交付 CoreProvider / Adapter Layer 的 turn 编排、治理、ToolResult/Artifact/Event/Trace mapping，不交付前端体验、不实接 piAgent、不实现长期记忆/RAG/Multi-Agent/浏览器自动操作。
```

False-green 审计：

```text
No false green for D mock-first scope.
Not yet product E2E: D 尚未接入既有 `/v1/chat/stream`，真实 Chrome + Runtime + B/C 渲染需要 V1.2-E Integration。
real piAgentProvider 仍是 No-Go；若后续未锁定 piAgent repo/version/license/runtime/tool invocation model 就声明 real provider 完成，应判定为重大规格偏差。
```

出门结论：

```text
Go for V1.2-C Mindmap Service fixture-first implementation.
No-Go for claiming full product E2E until V1.2-E.
```
