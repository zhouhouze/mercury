# V1.2-D AgenticLoop / Adapter Workspace 门禁

版本：V1.2-D Stage Gate Draft
日期：2026-06-04
负责模块：D，AgenticLoop ChatBox Core 与 MCP / Skill / API Adapter 编排

---

## 1. 阶段目标

将 D 模块定义为唯一编排核心，统一接入 A/C/Internal Tool/MCP/Skill/External API Adapter，并继续保证 state、budget、permission、ToolResult、ArtifactRecord、SSE 和 Trace 可审计。

---

## 2. 允许工作区

D 模块 Codex 只允许修改：

```text
services/local-runtime/navia_runtime/modules/agent_loop/
services/local-runtime/navia_runtime/modules/adapters/
docs/navia_v1_project_docs/stage-gates/v1.2-d-agentic-loop.md
```

禁止修改 B UI 实现、A/C 输出合同、公共 API 合同、`agent.py` 和 `tools.py` 既有入口。需要接入既有 Runtime 时由 Integration Codex wiring；需要变更合同时回到 V1.2-0。

---

## 3. 开发任务

- [ ] 保留 public `TurnRunner` 入口。
- [ ] 拆出 AgenticLoopContext。
- [ ] 建立 AdapterRegistry。
- [ ] 将既有 summarize / answer / explain / mindmap 映射为 internal tool adapters。
- [ ] 定义 MCP / Skill / External API safe adapter placeholder。
- [ ] 所有 adapter 经过 governance hooks。
- [ ] 所有 adapter 输出映射为 ToolResult。
- [ ] 单 Session 连续上下文接入 recent messages 和 checkpoint。

---

## 4. 验收标准

- [ ] 每个 user message 创建 `turn_id`。
- [ ] 每个 adapter call 创建 `tool_call_id`。
- [ ] Budget / Permission check 在 `tool.started` 前执行。
- [ ] denied adapter 不产生 `tool.started`。
- [ ] MCP / Skill / API 不绕过 D。
- [ ] trace 可按 `turn_id` 看到 state、intent、budget、tool、artifact、response、error。
- [ ] D 不实现长期记忆、RAG、多 Agent 或浏览器自动操作。

---

## 5. 预审计

当前结论：

```text
Blocked until V1.2-0 is accepted by project owner.
```

No-Go：

- D 默认执行高风险 MCP / Skill side effect。
- D 引入长期记忆或 RAG。
- D 允许前端直连 adapter。
- D 允许 AdapterResult 跳过 ToolResult envelope。
