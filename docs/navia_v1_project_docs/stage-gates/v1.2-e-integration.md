# V1.2-E Integration / E2E / PRD Review 门禁

版本：V1.2-E Stage Gate Draft
日期：2026-06-04
负责角色：Integration Codex

---

## 1. 阶段目标

Integration Codex 不开发 A/B/C/D 业务逻辑，只负责 wiring、合同一致性审计、真实数据端到端验收和 PRD 规格复检。

---

## 2. 允许工作区

Integration Codex 只允许修改：

```text
services/local-runtime/navia_runtime/app.py
services/local-runtime/navia_runtime/contracts.py
services/local-runtime/navia_runtime/stores.py
services/local-runtime/navia_runtime/agent.py
services/local-runtime/navia_runtime/tools.py
apps/chrome-extension/src/pageContext.ts
apps/chrome-extension/src/injectedPanel.ts
apps/chrome-extension/src/sse.ts
apps/chrome-extension/src/runtimeClient.ts
services/local-runtime/tests/integration/
apps/chrome-extension/e2e/
docs/navia_v1_project_docs/stage-gates/v1.2-e-integration.md
```

如果发现 A/B/C/D 合同不一致，Integration Codex 不得自行重写模块，应打回对应模块或 V1.2-0。

---

## 3. 集成任务

- [ ] 先按 `design/v1.2-integration-contract-matrix.md` 复核 A/B/C/D 合同一致。
- [ ] 先按 `design/v1.2-prd-coverage-matrix.md` 复核关键 PRD 体验均有 evidence 目标。
- [ ] 接通 A 输出到 D 的 StructuredPageContext。
- [ ] 接通 D AdapterRegistry 到 C mindmap adapter。
- [ ] 接通 D SSE 到 B renderer。
- [ ] 验证 MCP / Skill / API safe adapter placeholder 不执行高风险 side effect。
- [ ] 验证 trace 可还原完整 turn。
- [ ] 验证 Mindmap source map 可被 B 消费。

---

## 4. 端到端验收

真实数据主链路：

```text
打开真实网页
-> 读取网页
-> A 生成 StructuredPageContext
-> D 创建 turn
-> D 调用 summary adapter
-> B 流式渲染 summary
-> 用户追问
-> D 使用单 Session 历史和 activePage 回答
-> D 调用 C mindmap adapter
-> B 渲染 mindmap
-> 点击节点反跳 source paragraph 或展示 excerpt fallback
-> trace?turn_id 可审计
```

必须通过：

- [ ] Runtime tests。
- [ ] Extension unit tests。
- [ ] Typecheck。
- [ ] Chrome in-page E2E。
- [ ] 真实 Chrome 手工验收。
- [ ] PRD A-F 交互不回退。
- [ ] 更新 `design/v1.2-prd-coverage-matrix.md` 中实际 evidence 路径。

---

## 5. 预审计

当前结论：

```text
Blocked until V1.2-A/B/C/D are completed and individually accepted.
```

No-Go：

- Integration Codex 修改模块业务逻辑。
- 只用 mock 声明通过。
- 真实 Chrome 链路不通。
- trace 或 artifact source chain 缺失。
