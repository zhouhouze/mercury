# V1.1 Current Baseline Revalidation

版本：V1.1 Current Baseline Revalidation
日期：2026-06-05
状态：Passed

## 1. 复验目标

本轮复验用于确认当前仓库基线仍满足项目级 `V1.1 frontend fidelity ready` 的出门条件，并确认 A 模块 `A-V1.1` 高信号感知增强不会造成 V1.1 前端体验回退。

## 2. 执行命令

```bash
node scripts/validate_v1_1_doc_readiness.mjs
pnpm --dir apps/chrome-extension test
pnpm --dir apps/chrome-extension typecheck
pnpm --dir apps/chrome-extension build
NAVIA_E2E_HEADLESS=false pnpm --dir apps/chrome-extension e2e:inpage
pnpm --dir apps/chrome-extension e2e:visual
PYTHONPATH=services/local-runtime python3 -m pytest -q services/local-runtime/tests services/local-runtime/navia_runtime/modules/page_reading/tests
git diff --check
```

## 3. 当前结果

| 验收项 | 结果 |
|---|---|
| V1.1 文档 readiness | `status=go`, `canStartV11B=true` |
| Chrome extension unit tests | 5 files passed, 26 tests passed |
| Chrome extension typecheck | passed |
| Chrome extension build | passed |
| In-page Chrome E2E | passed |
| Visual E2E | passed |
| Runtime + A module tests | passed |
| diff whitespace check | passed |

## 4. 真实数据与真实环境

本轮使用：

- 真实 Chromium 窗口，`headless=false`。
- 真实 MV3 unpacked extension：`apps/chrome-extension/chrome-mv3-unpacked`。
- 真实 Local Runtime：`http://127.0.0.1:17861`。
- 真实 HTML fixture：`docs/navia_v1_project_docs/fixtures/real_pages/article.html`。
- Visual E2E 输出截图目录：`apps/chrome-extension/e2e/screenshots/current/`。

## 5. PRD 规格复检

| PRD 项 | 复检结论 |
|---|---|
| 页面贴边悬浮球 | 通过，floating default 和 hover 均有截图。 |
| Hover 小长条 | 通过，`Ask AI` strip 可触发打开。 |
| 440px push | 通过，网页内容被推挤且聊天主路径可用。 |
| 50vw push | 通过，半屏状态可用。 |
| Overlay | 通过，宽工作区不继续推挤页面。 |
| Mobile overlay | 通过，390x844 主路径可用。 |
| Runtime offline | 通过，错误可见且不伪造摘要。 |
| PageContext / summary / QA / mindmap artifact existence | 通过，功能 E2E 覆盖。 |

## 6. False-Green 审计

| 风险 | 结论 |
|---|---|
| 只靠历史文档声明 | 已规避，本轮重新执行命令。 |
| 只靠 mock DOM | 已规避，使用真实 Chrome MV3 extension。 |
| 只靠 Side Panel | 已规避，验收对象是网页内 injected panel。 |
| Runtime 未真实启动 | 已规避，E2E 使用本地 Runtime。 |
| A-V1.1 越界影响前端 | 已规避，A 模块测试与前端 E2E 均通过。 |
| Mermaid / Mindmap 视觉被误声明完成 | 已规避，仍只声明 artifact/source fallback，视觉专项后置。 |

## 7. 结论

```text
V1.1 frontend fidelity ready remains valid for the current local baseline.
A-V1.1 page perception enhancement is accepted within A module scope and does not regress V1.1 frontend fidelity.
```

仍不得声明：

```text
V1.1 complete with Mermaid visual fidelity.
V1.1 complete with ToolDock feature implementation.
```
