# V1.2-A Page Reading Workspace 门禁

版本：V1.2-A Stage Gate Draft
日期：2026-06-04
负责模块：A，网页信息提取、过滤、蒸馏与结构化总结

---

## 1. 阶段目标

把当前 PageContext 升级为高密度结构化网页事实源，为 D 的连续对话和 C 的 Mindmap 生成提供稳定输入。

必须输出：

```text
StructuredPageContext
ParagraphAnnotation[]
PageChunk[]
StructuredSummaryDraft optional
```

---

## 2. 允许工作区

A 模块 Codex 只允许修改：

```text
services/local-runtime/navia_runtime/modules/page_reading/
docs/navia_v1_project_docs/stage-gates/v1.2-a-page-reading.md
```

禁止修改：

```text
apps/chrome-extension/src/modules/chat_renderer/
apps/chrome-extension/src/modules/artifact_renderer/
services/local-runtime/navia_runtime/modules/mindmap/
services/local-runtime/navia_runtime/modules/agent_loop/
services/local-runtime/navia_runtime/modules/adapters/
apps/chrome-extension/src/pageContext.ts
services/local-runtime/navia_runtime/app.py
06-api-contract.md
07-data-models.md
contracts/v1_2_adapter_contracts.md
```

如需修改公共合同，必须停止并回到 V1.2-0。
如需接入既有 `pageContext.ts` 或 Runtime endpoint，必须交给 Integration Codex。

---

## 3. 开发任务

- [ ] 从真实 DOM 提取 title、url、domain、headings、visible text、cleaned text。
- [ ] 生成 paragraph blocks。
- [ ] 生成 heading tree。
- [ ] 生成 semantic chunks。
- [ ] 为 paragraph 标注 role、importance、densityScore、confidence。
- [ ] 生成 contentHash。
- [ ] 输出 StructuredSummaryDraft。
- [ ] 保持现有 `/v1/page/context` 外部 API 兼容。

---

## 4. 验收标准

- [ ] 使用真实 HTML fixture 验收 article / docs / github_readme。
- [ ] 使用真实 Chrome tab 验收一次 PageContext。
- [ ] 每个 paragraph 有稳定 `paragraphId`。
- [ ] chunks 可回指 paragraph 或 heading path。
- [ ] 缺少有效正文时返回 `PAGE_CONTEXT_REQUIRED`。
- [ ] 不创建 summary / answer / mindmap artifact。

---

## 5. 预审计

当前结论：

```text
Blocked until V1.2-0 is accepted by project owner.
```

No-Go：

- A 直接调用 MCP / Skill / API。
- A 直接生成最终 assistant response。
- A 修改 D/C/B 工作区。
- A 私自改变 `StructuredPageContext` 合同。
