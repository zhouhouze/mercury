# V1.1-A Stage Gate：Visual Baseline Freeze

版本：V1.1-A
日期：2026-06-03
状态：Go for V1.1-B；mindmap artifact 后续专项

---

## 1. 阶段目标

冻结 V1.1 前端高保真的视觉基线。该阶段只补齐 Figma / screenshot 证据和视觉 token，不改前端代码。

---

## 2. PRD 规格检视

P0 来源：

- `PRD/窗口交互_PRD.md`
- `design/v1.1-figma-baseline/capture-matrix.md`
- `design/v1.1-figma-baseline/capture-manifest.json`
- `design/v1.1-figma-baseline/manual-capture-runbook.md`

必须覆盖：

- A：悬浮球默认态。
- B：hover 小长条。
- C：`440px` 窄距 push。
- D：`50vw` 半屏 push。
- E：`>52vw` overlay。
- F：收起恢复。
- 错误态：Runtime offline。
- Artifact 态：Mermaid / source fallback；按用户要求后续专项，不阻塞 V1.1-B。

---

## 3. 开发计划

- 使用用户提供图片冻结 `floating-default` 和 `floating-hover`。
- 以 `PRD/窗口交互_PRD.md` 硬约束冻结 `panel-440-push`、`panel-50vw-push`、`panel-overlay`、`mobile-overlay`。
- 将 `runtime-offline` 改为 V1.1 独立设计验收，不要求标准原型审计。
- 将 `artifact-mindmap` 移出 V1.1-B/C 阻塞项，后续专项实现。
- 更新 `capture-manifest.json` 的 `requiredStates`、`captures` 和 `readinessGate`。

---

## 4. 验收标准

Go 条件：

- `capture-manifest.json` 可解析。
- 所有 blocking required states 均不再阻塞。
- `capture-manifest.json` 记录用户提供图片、PRD 硬约束、design-only、deferred scope 的依据。
- `artifact-mindmap` 明确后续专项，不作为 V1.1-B/C 开工门槛。
- `node scripts/validate_v1_1_doc_readiness.mjs` 输出 `canStartV11B=true`。

No-Go 条件：

- 用户提供的 Image #1/#2 未被登记。
- 3-6 状态未明确按 PRD 硬约束实现。
- `runtime-offline` 未有独立设计验收要求。
- `artifact-mindmap` 未明确后续专项，导致范围混淆。
- 只用 Side Panel 或 extension page 截图替代网页内体验。

---

## 5. 真实数据验收

必须使用真实已登录 Chrome 或可读取的真实 Figma `/design` 节点。不得用 mock 图、示意图或文字描述替代视觉基线。

---

## 6. 预审计意见

```text
Go for V1.1-B after machine readiness check passes.
Mindmap artifact visual implementation deferred.
```

重大风险：

| 风险 | 等级 | 闭环 |
|---|---|---|
| 用户图片未落盘为文件 | 中 | 以 manifest 的 `user_provided_prompt_image` 登记，不要求仓库文件 |
| PRD 硬约束被开发时改动 | 重大 | 3-6 必须按 `PRD/窗口交互_PRD.md` 和 `panelLayout.ts` 阈值实现 |
| Mindmap 被误认为 V1.1 必做 | 重大 | `artifact-mindmap` 已标记 deferred，不阻塞 V1.1-B/C |

---

## 7. 当前结论

```text
V1.1-A is complete for V1.1-B readiness.
V1.1-B may start after machine check returns canStartV11B=true.
```

机器校验：

```bash
node scripts/validate_v1_1_doc_readiness.mjs
```

当前预期为通过。若失败，必须先修复 manifest 或文档一致性。
